import { ItemsContainerProp } from "../../../types";

const Item: React.FC<ItemsContainerProp> = (props) => {
  const { accountData } = props;
  
  return (
    <div className="item">
      yo
    </div>
  );
};

export default Item;